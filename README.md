
  # EV Charging Prototype Development

  This is a code bundle for EV Charging Prototype Development. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
